-- ============================================================
-- 20 报价单：合同限期（成交后合同期限·月）
--   报价日期 quote_date / 报价有效期 expired_date 已存在，本次在前端补充录入
-- ============================================================
ALTER TABLE quotation
  ADD COLUMN IF NOT EXISTS contract_term integer; -- 合同限期（月）
